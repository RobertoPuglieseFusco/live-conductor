node_modules/
.DS_Store
*.log
npm-debug.log*
.env
.env.local

# Cycling '74's device — kept locally for reference, not ours to redistribute
*.amxd
# Ableton auto-backups and macOS cruft. The .als set itself IS versioned —
# it's the set the conductor targets, so the repo describes itself.
*Project/Backup/
Icon?
__pycache__/
*.pyc
# recorded audio doesn't belong in git
*Project/Samples/
